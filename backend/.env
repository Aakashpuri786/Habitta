# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection String (Replace with your own MongoDB URI)
# For local MongoDB use: mongodb://localhost:27017/habitta
# For MongoDB Atlas, replace with your own connection string:
MONGODB_URI=mongodb+srv://habitta_user:habitta_password@cluster.mongodb.net/habitta?retryWrites=true&w=majority

# JWT Configuration  
JWT_SECRET=habitta-jwt-secret-key-change-in-production
JWT_EXPIRE=7d

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173
